---
# This playbook runs entirely locally against the Palette and JumpCloud APIs.
# Only localhost is needed as a target.
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
